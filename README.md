# ✏️ Читмод — SillyTavern Extension

Плавающая панель управления стилем повествования, атмосферой и отношениями персонажей. Ползунки скрыто передают промпты нейросети.

---

## 📦 Установка

### Вариант 1 — через GitHub (рекомендуется)

1. Открой SillyTavern → **Extensions** → **Install Extension**
2. Вставь URL репозитория: `https://github.com/YOUR_USERNAME/cheatmod`
3. Нажми Install → перезагрузи страницу

### Вариант 2 — вручную

1. Скопируй папку `cheatmod` целиком в:
   ```
   SillyTavern/public/extensions/cheatmod/
   ```
2. Перезагрузи страницу SillyTavern
3. Зайди в **Extensions** → найди **Читмод** → включи

---

## 🎮 Использование

- Нажми кнопку **✏️ Читмод** (плавающая кнопка справа внизу), чтобы открыть панель
- **Ползунки** — двигай, чтобы регулировать интенсивность каждого параметра. При значении 0% ползунок не влияет на текст
- **➕ Добавить ползунок** — создай свой ползунок с любым названием и промптом
- **Заметки** — пиши любые инструкции для AI, они будут добавляться в промпт
- **Отношения** — добавляй пары персонаж↔пользователь, выбирай тип отношений и уровень привязанности
- **Отношения меняются автоматически** в процессе RP на основе тона реплик AI

---

## ⚙️ Редактирование промптов

Зайди в **Extensions** → **Читмод** → там можно:
- Изменить текст промпта для каждого ползунка
- Изменить название ползунка
- Использовать `{value}` для вставки процентов
- Использовать `{word}` для ползунков со словами

---

## 💾 Пресеты

- **Экспорт пресета** — сохраняет все настройки в `.json` файл
- **Импорт пресета** — загружает настройки из файла

---

## 🗂️ Структура файлов

```
cheatmod/
├── manifest.json   — метаданные расширения
├── index.js        — логика, UI, интеграция с ST
├── style.css       — стили (тёмная золотая тема)
└── README.md       — этот файл
```

---

## 🔧 Как работают скрытые промпты

Перед каждой генерацией текста расширение собирает все активные ползунки (значение > 0%) и вставляет их промпты в системное сообщение в блок `[ЧИТМОД ИНСТРУКЦИИ]...[/ЧИТМОД]`. Нейросеть видит эти инструкции, но они не отображаются в чате.

---

## ❤️ Автоматические отношения

После каждого ответа AI расширение анализирует текст на позитивные/негативные слова и корректирует уровень привязанности (+3 / -3 за каждое совпадение). Это базовая эвристика — её можно доработать в коде.
